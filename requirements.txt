# The receiver, recorder, and replay APIs have no third-party dependencies.
# Install optional features from the project root with one of:
#   pip install -e ".[retargeting]"
#   pip install -e ".[viewer]"
#   pip install -e ".[all]"
